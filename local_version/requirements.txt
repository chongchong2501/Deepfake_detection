# 深度伪造检测系统依赖 - 本地RTX4070版本
# 安装命令: pip install -r requirements.txt

# 核心深度学习框架
torch>=2.0.0,<2.2.0
torchvision>=0.15.0,<0.17.0
torchaudio>=2.0.0,<2.2.0

# 计算机视觉和图像处理
opencv-python>=4.8.0,<5.0.0
Pillow>=9.5.0,<11.0.0
albumentations>=1.3.0,<2.0.0

# 数据处理和科学计算
numpy>=1.21.0,<2.0.0
pandas>=1.5.0,<3.0.0
scipy>=1.9.0,<2.0.0

# 机器学习
scikit-learn>=1.3.0,<2.0.0

# 可视化
matplotlib>=3.6.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# 进度条和用户界面
tqdm>=4.64.0,<5.0.0

# 视频处理（可选，推荐）
av>=10.0.0,<12.0.0

# 系统监控
psutil>=5.9.0,<6.0.0

# 配置文件处理
PyYAML>=6.0,<7.0

# 数据验证
jsonschema>=4.17.0,<5.0.0

# 日志处理
coloredlogs>=15.0,<16.0

# 开发和调试工具（可选）
ipython>=8.0.0,<9.0.0
jupyter>=1.0.0,<2.0.0
ipywidgets>=8.0.0,<9.0.0

# 注意事项:
# 1. PyTorch相关包需要根据CUDA版本选择合适的安装源
# 2. 对于CUDA 11.8: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# 3. 对于CUDA 12.1: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# 4. 如果没有GPU，可以安装CPU版本: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# 5. av包在某些系统上可能需要额外的系统依赖，如果安装失败可以跳过